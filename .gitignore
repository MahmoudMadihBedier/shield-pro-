# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
coverage

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Env: `.env` and `.env.example` hold only non-secret Appwrite client config
# and ARE committed. Real secrets live in these, which are not:
.env.local
.env.*.local

# Vercel
.vercel

# Claude Code worktrees for parallel agents — transient checkouts
.claude/worktrees/
