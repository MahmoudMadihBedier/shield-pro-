/**
 * Blank line-item rows shared by the editors and the form pages (kept in a
 * component-free file so the editor modules export only components).
 */
export const EMPTY_PO_LINE = { raw_material_id: '', qty: 1, unit_price: 0 }
export const EMPTY_RECEIPT_LINE = { raw_material_id: '', qty: 1, unit_price: 0 }
