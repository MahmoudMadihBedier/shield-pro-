# Copy to `.env.local` (git-ignored). Holds SECRETS — never commit this file.

# Appwrite server API key for provisioning + Functions + CI.
# Console → Project → Overview → Integrations → API Keys → Create API key.
# Scopes needed by scripts/appwrite/provision.ts:
#   databases.read, databases.write,
#   tables.read, tables.write,
#   collections.read, collections.write,
#   attributes.read, attributes.write,
#   indexes.read, indexes.write,
#   documents.read, documents.write,
#   teams.read, teams.write
APPWRITE_ENDPOINT="https://fra.cloud.appwrite.io/v1"
APPWRITE_PROJECT_ID="6a95b631003d4163dc97"
APPWRITE_API_KEY=""
