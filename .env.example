# Copy to `.env` (already committed with the shared cloud project values) or
# `.env.local` to point at your own Supabase project.
#
# Non-secret public client config — safe to ship in the browser bundle.

VITE_SUPABASE_URL="https://ajrevsyyudfjrwiifekj.supabase.co"
VITE_SUPABASE_PUBLISHABLE_KEY="sb_publishable_..."
