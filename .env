# Supabase client configuration for Shield Pro.
#
# These two values are NON-SECRET public client config (they are shipped in the
# browser bundle by design, exactly like a Firebase config object). They are
# committed on purpose so the app builds anywhere without extra setup.
#
# Anything that is actually secret (the service-role key, the DB password, OAuth
# secrets) MUST go in `.env.local`, which is git-ignored.

VITE_SUPABASE_URL="https://ajrevsyyudfjrwiifekj.supabase.co"
VITE_SUPABASE_PUBLISHABLE_KEY="sb_publishable_Sm6F2ep7vQ4VttjfPKAKlw_2f0V1heT"
